# this folder contains simulation snapshots

The .hdf5 are the raw simulation snapshot data

outputlist.txt file containts the scale factor when the snapshots are output.

# Python Packages

You will need to install some python packages for the practices:

h5py, numpy, scipy, matplotlib, nbodykit


[simulation snapshot 2](https://www.dropbox.com/s/msr5yq4i1bph0ey/snap_002.hdf5?dl=0)
